---
label: Menu
hide_body: true
is_partial: true
display_field: nav_menu
fields:
- type: select
  config:
    source:
      type: simple
    options:
    - home
  label: Nav Menu
  name: nav_menu
  description: The menu to display in the nav bar

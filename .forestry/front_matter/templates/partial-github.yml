---
hide_body: false
is_partial: true
fields:
- type: text
  name: id
  label: Section ID
  description: 'The unique section used to reference in #id-links'
  config:
    required: true
- type: text
  label: Title
  name: title
  description: Optional title displayed above the collections
- type: field_group_list
  label: Repos
  name: repos
  description: The repos to display
  fields:
  - type: text
    label: Repo
    name: repo
    description: e.g. SwiftCommonMark
    config:
      required: true
  - type: text
    name: owner
    label: Repo Owner
    description: e.g. codytwinton
    config:
      required: true

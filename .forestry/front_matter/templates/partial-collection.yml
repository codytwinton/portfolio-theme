---
hide_body: false
is_partial: true
fields:
- type: text
  name: id
  label: Section ID
  description: 'The unique section used to reference in #id-links'
  config:
    required: true
- type: text
  name: title
  label: Title
  description: The optional title displayed above the collections
- type: select
  name: collection
  config:
    source:
      type: simple
    required: true
    options:
    - work
  label: Collection
  description: The collection to display
  default: work
